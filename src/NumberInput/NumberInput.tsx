import React, { PureComponent, ReactNode } from 'react'

//TASK create an input component that accepts number values an emits onChange events that have already handled the number conversion

class NumberInput extends PureComponent<{todo: 'TODO'}> {
  public render(): ReactNode {
    return <></>
  }
}

export default NumberInput